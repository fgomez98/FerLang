start
    # esto es un comentario #
    var x = 0;
    var y = 1;
    let text = "Enter a number: ";
    put ("%s",  text);
	getnum ( x );
	var num = 0;
	num = x;
  	while (x > 0) {
   		y = y * x;
   		x = x - 1;
   	}
  	put ( "factorial of %d is %d\n", num, y);
end
