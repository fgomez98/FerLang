start
    var x:number;
    var c = 0;
    put ("Enter an integer: ");
    getnum(x);
    if (x == 0) {
	 put ("Number of digits: %d\n", 1);
	} else {
        while ( x != 0) {
            x = x / 10;
	        c ++;
        }
        put ("Number of digits: %d\n", c);
    }
end